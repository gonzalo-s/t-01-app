import React from "react";
import { View, Text } from "react-native";

const EnterCodeScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Ingresar codigo de turno</Text>
    </View>
  );
};

export default EnterCodeScreen;
